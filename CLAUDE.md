# CLAUDE.md

Guidance for AI assistants (and humans) working in this repository.

## Current state

> **This repository is currently empty.** As of this file's creation there is no
> source code, build configuration, or history — the initial commit is this
> `CLAUDE.md` itself.
>
> Because there is no codebase to describe, the sections below are a **scaffold**.
> Fill each one in as real structure lands. Do not document components, commands,
> or conventions that do not yet exist — an accurate short doc is far more useful
> to an AI assistant than an aspirational long one. Delete this note once the
> repository has meaningful content.

## How to keep this file accurate

When you add code to this repository, update the matching section below in the
same change. Prefer describing what the code *does* and the commands that
*actually work* over restating intentions. If a section has nothing true to say
yet, leave its placeholder in place rather than inventing content.

## Project overview

<!-- One or two sentences: what this project is and who/what it serves. -->
_Not yet defined._

## Repository structure

<!--
Once directories exist, describe the top-level layout and what lives where, e.g.:

- `src/`      — application source
- `tests/`    — test suite
- `scripts/`  — dev/ops tooling
-->
_Not yet defined — the repository has no files besides this one._

## Development workflow

<!--
Document the real, verified commands once tooling is set up. For example:

- Install dependencies: `...`
- Run the app locally:  `...`
- Run tests:            `...`
- Lint / format:        `...`
- Build:                `...`

Only list commands you have confirmed run in this repo.
-->
_No build system, dependency manifest, or task runner is present yet._

## Key conventions

<!--
Capture project-specific conventions an assistant would otherwise get wrong:
naming, formatting rules, commit message style, error-handling patterns,
where new modules go, testing expectations, etc.
-->
_None established yet._

## Git and branching

- Feature/development work happens on dedicated branches (do not commit directly
  to the default branch).
- Write clear, descriptive commit messages.
- Open a pull request only when explicitly requested.

## Notes for AI assistants

- The repository has essentially no context to draw on yet. Before assuming a
  language, framework, or layout, check what files actually exist.
- When you introduce the first real code, replace the placeholders above with
  concrete, verified information — this is the highest-leverage way to keep this
  file useful.
